[project]
name = "ai-banking-lab"
version = "0.1.0"
description = "Synthetic banking data generators for analytics and GenAI practice."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
